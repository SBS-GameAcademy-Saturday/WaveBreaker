# Phase 10 · 배포 · 발표 ✅

| | |
|---|---|
| **회차 / 주차** | 126–130 (5회) / 26주차 |
| **선행** | Phase 9 |
| **산출물** | 공유 링크 + 시연 영상 + 발표 |

## 🎯 목표

1. 최종 빌드를 산출한다
2. **친구·가족에게 링크로 보낸다** ← 이 수업의 진짜 보상
3. 포트폴리오가 될 형태로 정리한다

## 🗓 회차 배분

| 회차 | 내용 |
|---|---|
| 126 | 최종 빌드(PC) + 🔴 **기능 동결** + 빌드 리포트 읽기 |
| 127 | itch.io 업로드 → **링크 발급** |
| 128 | 시연 영상·GIF + 소개글 작성 |
| 129 | 발표 준비 + **리허설** (2인 1조 상호 피드백) |
| 130 | **최종 발표회** (1인 5분) + 원본 백업 + 과정 종료 |

> 🔴 **126회차부터 기능 동결이다.** 원래 배분은 129–130 두 회차를 발표회로 잡았지만,
> **리허설을 안 하면 5분이 시연 준비로 다 날아간다.** 129를 준비·리허설로, 130을 발표회로 나눴다.
> 인원이 많으면 130 하나로는 부족하니 129 후반을 발표 1부로 쓴다.

## ✅ 종료 조건

- [x] 최종 빌드가 정상 실행된다 (싱글 + 협동 모두) — 126회차 실측 `Succeeded / 에러 0 / 49.6초 / 108.5 MB`
- [ ] 공유 가능한 링크가 있다 — **강사 환경 미실측** (itch.io 업로드는 학생 계정으로 한다)
- [ ] 시연 영상 또는 GIF 가 있다 — **미실측** (Win+G 녹화는 수업 중 진행)
- [ ] 발표를 마쳤다 — 수업 진행 항목

> 아래 두 항목은 **강사 환경에서 재지 않았다.** 계정 생성·외부 업로드·화면 녹화가 필요해서다.
> 대신 업로드에 올릴 **산출물**은 실측했다 — zip 41.3 MB, 스크린샷 2장.

## 📦 최종 산출물 (1인 기준)

게임 빌드(PC) · itch.io 링크 · 시연 영상 · 프로젝트 원본 압축본 · `Snapshot_P8_Final_Single` 백업본

**강사 환경 실측 (2026-09-03)**

| 산출물 | 크기 | 비고 |
|---|---|---|
| 빌드 폴더 | **108.8 MB** · 245개 파일 | 21주차(3씬) 105.2 MB · 203개 → **협동 전체가 +3.6 MB** |
| 배포용 zip | **41.3 MB** | 62.1% 줄었다. itch.io 무료 한도 1 GB 이내 |
| 원본 zip | **2.5 MB** | `Assets`+`ProjectSettings`+`Packages` 만 |
| (참고) `Library` | **3,206.7 MB** | 넣으면 **1310배**가 된다 |

> 🔴 **WebGL 은 산출물에서 뺐다.** 강사 환경에서 `BuildPipeline.IsBuildTargetSupported(WebGL) = False` —
> 모듈이 안 깔려 있다. `Android = True` 라서 모바일 쪽이 오히려 열려 있다(130회차 '앞으로의 길').

## 🚨 위험 신호 (강사용)

| 신호 | 의미 | 대응 |
|---|---|---|
| WebGL 빌드에서 하루 날림 | 흔한 함정 | **PC 빌드를 먼저 확정.** WebGL은 되면 좋고 안 되면 생략 |
| 업로드 후 다운로드 버튼이 없음 | 🔴 **플랫폼 체크 누락** | Uploads 에서 ☑ Windows |
| 소개글에 사과가 들어감 | 매우 흔함 | 그 자리에서 지우게 한다 (128) |
| 백업 영상 없이 발표 당일 | 시연 실패 시 발표 불가 | **129회차에 전원 준비 강제** |
| 압축본이 3 GB | `Library` 포함 | 세 폴더만 (130) |
| 발표를 부담스러워함 | 연령대 혼합 반에서 흔함 | **녹화 영상 제출로 대체 허용.** 강요하지 않는다 |
| "취업에 쓸 수 있나요" | 좋은 질문 | 에셋이 CC0라 포트폴리오·스토어 이용 가능. 폰트 라이선스만 별도 확인 |
| 마지막에 급하게 기능 추가 | 위험 | **126회차부터 기능 동결** |

## ⏭ 이후

버퍼 131–140회는 밀린 진도 회수, 개인 확장, 또는 2인 대전 모드 추가에 쓴다.
