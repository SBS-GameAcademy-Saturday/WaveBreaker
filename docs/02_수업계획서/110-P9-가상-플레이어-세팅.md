# 110회차 · 가상 플레이어 세팅 — 혼자서 둘이 한다

| | |
|---|---|
| **Phase** | 9 · 네트워크 협동 |
| **소요** | 2시간 (비대면) |
| **선행** | 109회차 (첫 접속) |
| **오늘 배우는 것** | Multiplayer Play Mode(MPPM), 가상 플레이어, 태그 |
| **씬** | `Practice/109_Network_Test.unity` |
| **준비물(강사)** | ⚠️ **학생 램 용량 파악.** 8GB 이하는 대안이 필요하다 |

## 🎯 오늘의 목표

1. **가상 플레이어**를 하나 띄운다
2. MPPM 의 **제약**을 미리 안다
3. `Snapshot_P9_step1` 을 배포받는다

> ⚠️ **오늘 2인 접속까지는 안 간다.** 세팅까지다. 실제 2인 테스트는 111회차(다음 주)다.
> 오늘 세팅에서 막히면 다음 주가 통째로 날아가므로 **한 회차를 여기에 쓴다.**
>
> 🚨 **가상 플레이어는 유니티를 하나 더 켜는 것이다.** 램을 3GB 정도 더 쓴다.
> 실측: 메인 에디터 3.5GB + 가상 플레이어 3.3GB.

## 📦 오늘의 제출물

**가상 플레이어 창이 떠 있는 스크린샷 (창 제목이 `Player 2`)** → `#자랑`

---

## ⏱ 타임테이블

| 시간 | 블록 |
|---|---|
| 00:00–00:10 | 복습 퀴즈 + **램 확인** |
| 00:10–00:50 | 같이 하기 — MPPM 창 → 활성화 → 제약 |
| 00:50–01:00 | 휴식 |
| 01:00–01:30 | 개인 미션 |
| 01:30–01:50 | 순회 확인 + **22주차 회고** |
| 01:50–02:00 | 체크리스트 + `Snapshot_P9_step1` |

---

## 00:00–00:10 · 복습 퀴즈 + 램 확인

### 복습 퀴즈 (4분)

1. 네트워크 값은 어느 함수부터 유효한가? → **`OnNetworkSpawn`** (109)
2. 접속하면 캐릭터를 누가 만드나? → **유니티가** (Player Prefab) (109)
3. [서버] 를 누르면 왜 캐릭터가 없나? → **서버는 플레이어가 아니다** (107·109)

### 🚨 램 확인 (6분)

**전원이 자기 램 용량을 채팅에 쓴다.** (`작업 관리자 → 성능 → 메모리`)

| 램 | 대응 |
|---|---|
| 16GB 이상 | 문제없다 |
| 8~16GB | 다른 프로그램을 다 끄고 한다 |
| **8GB 이하** | 🚨 **가상 플레이어가 버겁다.** 아래 대안 |

**대안** — 가상 플레이어를 못 쓰는 학생

1. **빌드로 테스트한다**: 에디터에서 [호스트], 빌드한 `.exe` 에서 [클라이언트]
2. 짝을 지어 **한 명 컴퓨터에서** 같이 본다

> 💬 "빌드 테스트는 104회차에서 이미 해봤죠. **그 방법이 여기서 또 쓰입니다.**"

---

## 00:10–00:50 · 같이 하기 ★

### ① MPPM 이란 (8분)

> 💬 "네트워크 테스트를 하려면 **게임이 두 개** 돌아야 합니다. 106회차에서 배운 그거요."
> 💬 "그런데 친구를 매번 부를 수는 없잖아요."

| 방법 | 어떻게 |
|---|---|
| 친구를 부른다 | 매번 시간을 맞춰야 한다 |
| 빌드해서 두 개 실행 | 고칠 때마다 다시 빌드 (**느리다**) |
| **가상 플레이어** | ✅ 에디터가 두 번째 유니티를 띄워준다 |

> 🔑 💬 "**가상 플레이어는 같은 프로젝트를 보는 두 번째 유니티**입니다."
> 💬 "코드를 고치면 **양쪽에 같이 반영됩니다.** 빌드를 안 해도 돼요."

### ② 창을 연다 (8분)

```
Window → Multiplayer → Multiplayer Play Mode
```

> 🚨 **메뉴가 안 보이면 108회차의 재시작을 안 한 것이다.**
> 강사가 실제로 겪었다 — 설치 직후엔 메뉴가 없고, **껐다 켜야 생긴다.**

**창에 보이는 것**

| 이름 | 뜻 |
|---|---|
| `Main Editor` | 지금 쓰고 있는 이 유니티 |
| `Player 2` `Player 3` `Player 4` | 띄울 수 있는 가상 플레이어 |

각 줄에 **활성화 체크박스**와 **역할(Role)** 이 있다.

| Role | 뜻 |
|---|---|
| **Client And Server** | 호스트로도 클라이언트로도 쓸 수 있다 (기본) |
| Client | 클라이언트 전용 |
| Server | 서버 전용 |

> 💬 "기본값 `Client And Server` 로 둡니다. **버튼으로 정할 거니까요.**"

### ③ `Player 2` 를 켠다 (12분)

`Player 2` 의 체크박스를 켠다.

**무슨 일이 나나**

```
상태 : NotLaunched  →  Launching  →  Launched
```

> 🚨 **처음 켤 때 몇 분 걸린다.** 프로젝트를 한 번 더 읽어들이기 때문이다.
> 두 번째부터는 빠르다.

**실측** (강사 컴퓨터)

```
Player 2 상태 = Launched
창 제목      = Player 2
메모리       = 3.3 GB   (메인 에디터는 3.5 GB)
```

> 💬 "**유니티 창이 하나 더 떴죠.** 제목이 `Player 2` 입니다."
> 💬 "이게 두 번째 게임이에요. 106회차의 그 그림이 지금 화면에 있는 겁니다."

### ④ 🚨 MPPM 의 제약 (12분) — 오늘 반드시 말해야 하는 것

> 🔴 💬 "**가상 플레이어에서는 못 하는 게 있습니다.** 이걸 모르면 30분을 날립니다."

| 못 하는 것 | 어떻게 |
|---|---|
| **GameObject 만들기 · 고치기** | **메인 에디터에서** 한다 |
| **Package Manager** | 메인 에디터에서 |
| 프로젝트 설정 변경 | 메인 에디터에서 |
| **씬 저장** | 메인 에디터에서 |

| 되는 것 |
|---|
| Play / Stop |
| Console 보기 |
| Hierarchy · Inspector **보기** |
| 게임 조작 |

> 🔑 💬 "**가상 플레이어는 '보는 창' 입니다.** 고치는 건 항상 메인 에디터예요."
> 💬 "가상 플레이어에서 오브젝트를 옮겼는데 저장이 안 된다? **정상입니다.**"

> 💡 코드(`.cs`)는 **양쪽이 같은 파일**을 본다. 그래서 코드를 고치면 둘 다 바뀐다.
> 씬과 프리팹도 같은 파일이지만, **저장은 메인에서만** 된다.

### ⑤ 태그 — 누가 누군지 구분하기 (5분)

가상 플레이어마다 **태그**를 붙일 수 있다. 코드에서 읽어 다르게 동작시킬 수 있다.

```
Player 2  →  태그 "Client"
```

> 💬 "예를 들어 '태그가 Client 면 자동으로 클라이언트로 접속' 하게 만들 수 있어요."
> 💬 "**오늘은 안 합니다.** 111회차에 버튼을 손으로 눌러 보고, 귀찮아지면 그때 자동화합니다."

### ⑥ 끌 때 (3분)

체크박스를 다시 끄면 `Deactivate` 된다. 상태가 `NotLaunched` 로 돌아간다.

> 💡 **수업이 끝나면 끄는 걸 권한다.** 램을 3GB 계속 쓰고 있다.

---

## 01:00–01:30 · 개인 미션

### 필수 미션

```
// TODO ①: Window → Multiplayer → Multiplayer Play Mode 를 연다
//         (메뉴가 없으면 유니티를 껐다 켠다)
// TODO ②: Player 2 를 활성화한다
// TODO ③: 창이 뜨고 제목이 "Player 2" 인지 확인한다
// TODO ④: 가상 플레이어에서 오브젝트를 하나 만들어 보고 안 되는 걸 확인한다
// TODO ⑤: 메인 에디터에서 스크립트를 고치고, 가상 플레이어에도 반영되는지 본다
// TODO ⑥: Player 2 를 끈다
```

<details>
<summary>막히면 열기 (정답)</summary>

**메뉴가 없다**
108회차 재시작을 안 한 것이다. `File → Exit` 후 다시 연다.

**활성화했는데 창이 안 뜬다**
- 몇 분 기다린다 (처음 한 번은 오래 걸린다)
- 작업 관리자에 `Unity.exe` 가 하나 더 있는지 본다
- 램이 부족하면 안 뜬다 → 대안(빌드 테스트)으로 간다

**⑤ 확인하는 법**

```csharp
// 메인 에디터에서 NetworkPlayerTag.cs 에 한 줄 추가
Debug.Log("코드가 양쪽에 반영됩니다");
```

저장하면 **가상 플레이어 쪽도 컴파일**된다. 양쪽 Console 에서 확인한다.

**가상 플레이어에서 뭘 확인하면 좋나**

| 창 | 볼 것 |
|---|---|
| Console | 로그가 따로 나온다 (**각자 게임이니까**) |
| Hierarchy | Play 중 오브젝트가 보인다 |
| Game | 이쪽 화면 |

</details>

**확인 조건**: 창 제목이 `Player 2` 인 유니티가 하나 더 떠 있다.

### ⭐ 도전 미션

- [ ] 가상 플레이어에서 오브젝트를 만들어 보고 **어떤 메시지가 나오는지** 적는다
- [ ] `Player 3` 까지 켜본다 (램을 보면서)
- [ ] 태그를 붙이고 `CurrentPlayer.ReadOnlyTags()` 로 읽어본다
- [ ] 메인과 가상 플레이어의 Console 이 **왜 따로인지** 106의 그림으로 설명한다
- [ ] 가상 플레이어에서 Play 를 눌러본다 (메인과 따로 도는가?)
- [ ] 램 사용량을 재서 적는다

> 💡 네 번째가 106회차와 이어지는 지점이다. "게임이 두 개니까 로그도 두 개."

---

## 01:30–01:50 · 순회 확인 + 22주차 회고

### 순회 (10분)

**화면 공유로** 확인한다.

| 확인 | |
|---|---|
| MPPM 창이 열리나 | 재시작을 했는가 |
| `Player 2` 가 `Launched` 인가 | |
| 램이 버티나 | 8GB 이하 학생은 대안으로 |

### 🎓 22주차 회고 (10분)

| 회차 | 한 것 | 새로 배운 것 |
|---|---|---|
| 106 | 왜 멀티가 어려운가 | **게임이 두 개** |
| 107 | 호스트 · 클라이언트 | **권한(authority)** |
| 108 | 패키지 설치 | `NetworkManager` · `UnityTransport` |
| 109 | 첫 접속 | `NetworkObject` · `OnNetworkSpawn` |
| 110 | 가상 플레이어 | MPPM |

> 💬 "이번 주에 **게임 기능은 하나도 안 늘었습니다.** 화면에 네모 하나 생긴 게 전부죠."
> 💬 "그런데 **20회차짜리 구간의 바닥을 깔았습니다.**"
>
> 🔑 💬 "그리고 **`Game.unity` 는 한 번도 안 건드렸습니다.**
> 여러분의 완성작은 지난주 그대로예요. 그게 이번 Phase 의 규칙입니다."

### 📦 `Snapshot_P9_step1` 배포

**패키지 6개 + 연습 씬 + `NetworkManager` + `NetworkPlayer` 프리팹.**

> 💬 "네트워크는 한 번 꼬이면 혼자 못 풉니다. **5회차마다 스냅샷을 드립니다.**"

---

## ✅ 체크리스트 (학생)

- [ ] `Window → Multiplayer → Multiplayer Play Mode` 가 열린다
- [ ] `Player 2` 가 `Launched` 상태다
- [ ] 창 제목이 `Player 2` 인 유니티가 떠 있다
- [ ] 가상 플레이어에서 **오브젝트를 못 고친다는 걸** 확인했다
- [ ] 코드는 양쪽에 같이 반영되는 걸 확인했다
- [ ] 램이 부족하면 **대안**을 안내받았다
- [ ] 🔴 **`Game.unity` 를 안 건드렸다**
- [ ] `Snapshot_P9_step1` 을 받았다
- [ ] 제출했다

## 🚨 흔한 사고 & 대응 (강사용)

| 에러/상황 | 진짜 원인 | 대응 |
|---|---|---|
| **MPPM 메뉴가 없음** | 108 재시작 안 함 | **오늘 1등.** 껐다 켠다 |
| 활성화해도 창이 안 뜸 | 첫 실행이 오래 걸린다 | 몇 분 기다린다 |
| 컴퓨터가 멈춤 | 램 부족 | **대안(빌드 테스트)** 으로 |
| **가상 플레이어에서 수정이 안 됨** | **MPPM 제약** | **정상.** 메인에서 한다 |
| 가상 플레이어에서 저장이 안 됨 | 위와 같음 | 정상 |
| Console 이 따로 나옴 | 게임이 두 개다 | 106 회수. **정상** |
| 가상 플레이어가 안 꺼짐 | 프로세스가 남았다 | 작업 관리자에서 `Unity.exe` 종료 |
| 패키지 폴더에 코드가 없음 | MPPM 은 문서만 | 정상. 기능은 에디터 내장 |
| 진도가 밀림 | 램 문제로 오래 걸림 | **세팅만 되면 오늘은 성공** |

## 📮 다음 주 예고 — 23주차 (111–115)

> "도구가 다 갖춰졌습니다. 다음 주부터 **진짜 협동**입니다."
>
> "111회차에 **두 명이 동시에 접속**합니다. 네모가 두 개가 돼요."
>
> "112회차엔 **움직입니다.** 그런데 그때 사고가 하나 납니다 —
> **내가 움직이면 상대도 같이 움직입니다.**"
>
> 🔑 "그걸 고치는 게 114회차인데, **한 줄이면 끝납니다.**
> 14주차(067)에 입력과 이동을 나눠둔 덕분이에요. **그때 왜 그랬는지 그날 알게 됩니다.**"
