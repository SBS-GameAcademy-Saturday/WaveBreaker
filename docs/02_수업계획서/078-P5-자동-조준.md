# 078회차 · 가장 가까운 적을 스스로 찾는다

| | |
|---|---|
| **Phase** | 5 · 본 프로젝트 코어 |
| **소요** | 2시간 (비대면) |
| **선행** | 077회차, **064회차 (자동 발사)**, 075회차 (`OverlapCircleAll`) |
| **오늘 배우는 것** | 최솟값 찾기 재확인, `Gizmos` 로 디버그 |
| **씬** | `Game.unity` |
| **준비물(강사)** | **064회차 `AutoShooter.cs` 를 열어둔다**, Scene 뷰 준비 |

## 🎯 오늘의 목표

1. 사거리 안에서 **가장 가까운 적**을 찾는다
2. 찾은 적을 **Scene 뷰에 선으로** 그린다
3. 아직 **쏘지는 않는다** — 조준까지만

> ⚠️ **오늘 안 하는 것**: 발사(079). 오늘은 **누구를 볼지 정하는 것**까지다.
>
> 🔑 **설계 의도**: 발사까지 한 번에 하면 **뭐가 틀렸는지 못 찾는다.**
> 엉뚱한 적을 쏘는데 조준이 틀린 건지 발사가 틀린 건지 알 수가 없다.
> 그래서 오늘은 **조준만 만들고 눈으로 확인**한다. `Gizmos` 를 쓰는 이유가 여기에 있다.

## 📦 오늘의 제출물

**Scene 뷰에서 조준선이 가장 가까운 적을 따라가는 GIF (10초)** → `#자랑`

---

## ⏱ 타임테이블

| 시간 | 블록 |
|---|---|
| 00:00–00:12 | 복습 퀴즈 + 064 다시 보기 |
| 00:12–00:50 | 같이 하기 — 사거리 → 최솟값 → Gizmos |
| 00:50–01:00 | 휴식 |
| 01:00–01:35 | 개인 미션 |
| 01:35–01:50 | 데모 + 정리 |
| 01:50–02:00 | 체크리스트 + 제출 |

---

## 00:00–00:12 · 복습 퀴즈 + 064 다시 보기

### 복습 퀴즈 (5분)

1. 원 안의 Collider 를 전부 가져오는 함수는? → **`Physics2D.OverlapCircleAll`** (075)
2. 두 점 사이 거리는? → **`Vector2.Distance`** (064)
3. 배열에서 최솟값을 찾는 방법은? → **처음엔 아주 큰 값, 더 작은 게 나오면 갈아치운다** (4주차)

### 064 를 연다 (7분)

`AutoShooter.cs` 의 `FindNearestEnemy` 를 띄운다.

> 💬 "13주차에 이거 만들었죠. **오늘 하는 게 이겁니다.**"
> 💬 "그런데 그때랑 다른 게 하나 있어요."

| | 064 (미니게임) | 078 (본 프로젝트) |
|---|---|---|
| 적을 찾는 법 | `FindGameObjectsWithTag` — **씬 전체** | `OverlapCircleAll` — **사거리 안만** |
| 적이 몇 마리? | 많아야 10마리 | **100마리 넘는다** |

> 💬 "씬 전체를 매번 뒤지면 몬스터가 100마리일 때 느려집니다."
> 💬 "**사거리 밖은 어차피 못 쏘잖아요.** 볼 필요가 없어요."

---

## 00:12–00:50 · 같이 하기 ★

### ① 사거리 안을 본다 (8분)

`Player` 에 `Scripts/Weapon/AutoGun.cs` 를 붙인다.

```csharp
using UnityEngine;

public class AutoGun : MonoBehaviour
{
    [SerializeField] private float range = 8f;

    public Transform CurrentTarget { get; private set; }

    private void Update()
    {
        CurrentTarget = FindNearest();
    }

    private Transform FindNearest()
    {
        Collider2D[] hits = Physics2D.OverlapCircleAll(transform.position, range);

        Debug.Log($"사거리 안 Collider {hits.Length}개");

        return null;
    }
}
```

▶ Play → Console 에 숫자가 계속 찍힌다.

> 💬 "적만 세고 싶은데 **바닥이나 칼도 세고 있죠.** 걸러내야 합니다."

### ② 가장 가까운 적을 고른다 (14분) — 오늘의 핵심

```csharp
private Transform FindNearest()
{
    Collider2D[] hits = Physics2D.OverlapCircleAll(transform.position, range);

    float minDistance = float.MaxValue;
    Transform nearest = null;

    foreach (Collider2D hit in hits)
    {
        if (!hit.CompareTag("Enemy")) continue;

        float distance = Vector2.Distance(transform.position, hit.transform.position);

        if (distance < minDistance)
        {
            minDistance = distance;
            nearest = hit.transform;
        }
    }

    return nearest;
}
```

**칠판 — 4주차 콘솔과 나란히 놓는다**

```
콘솔 (4주차)                          지금 (078)
────────────────────                  ────────────────────
int min = scores[0];                  float minDistance = float.MaxValue;
                                      Transform nearest = null;
for (i)                               foreach (hit)
  if (scores[i] < min)                  if (distance < minDistance)
    min = scores[i];                    { minDistance = distance;
                                          nearest = hit.transform; }
```

> 💬 "**똑같습니다.** 다른 건 두 가지예요."
> 💬 "① 비교하는 게 숫자가 아니라 **거리**. ② 값뿐 아니라 **누구였는지**도 기록."

> 🚨 **`float.MaxValue` 로 시작하는 이유** — 반드시 묻는다.
> "첫 번째 적이 **무조건 이기게** 하려고요. 콘솔에서 `scores[0]` 으로 시작한 거랑 같은 이유입니다."
> `0f` 로 시작하면? **아무도 못 이긴다.** 한 번 해보여준다.

> 🚨 **`nearest = null` 로 시작하는 이유**
> 사거리 안에 적이 하나도 없을 수 있다. 그때 `null` 이 돌아와야 "없다" 를 알 수 있다.

### ③ 눈으로 확인한다 (10분) — Gizmos

```csharp
private void OnDrawGizmosSelected()
{
    Gizmos.color = Color.cyan;
    Gizmos.DrawWireSphere(transform.position, range);

    if (CurrentTarget != null)
    {
        Gizmos.color = Color.red;
        Gizmos.DrawLine(transform.position, CurrentTarget.position);
    }
}
```

▶ Play → **Scene 뷰**를 열고 `Player` 를 선택한다.

- **하늘색 원** = 사거리
- **빨간 선** = 지금 조준 중인 적

> 🎉 여기가 오늘의 정점이다. 선이 가장 가까운 적을 따라 **탁탁 옮겨 다닌다.**

> 💬 "숫자로 `Debug.Log` 를 찍는 것보다 **선 하나 그리는 게 100배 빠릅니다.**"
> 💬 "**보이는 걸 만들면 디버깅이 쉬워집니다.** 이건 앞으로 계속 쓸 습관이에요."

| 함수 | 언제 그려지나 |
|---|---|
| `OnDrawGizmos` | **항상** (선택 안 해도) |
| `OnDrawGizmosSelected` | **선택했을 때만** |

> ⚠️ Gizmos 는 **Scene 뷰에만** 나온다. Game 뷰에는 안 보인다.
> 빌드한 게임에도 안 나온다. **개발용 표시**다.

### ④ `Update` 에서 매 프레임 찾아도 되나 (6분)

> 💬 "지금 **매 프레임** 사거리 안을 훑고 있습니다. 괜찮을까요?"
> 💬 "몬스터가 100마리면 100번 거리 계산을 60번/초 합니다. **6000번이죠.**"
>
> 💬 "지금은 괜찮습니다. 그런데 **다음 시간에 코루틴으로 옮길 거예요.**
> 어차피 0.5초에 한 번만 쏘는데, 매 프레임 찾을 이유가 없거든요."

> 💡 **미리 답을 주지 않는다.** 학생이 "그럼 왜 지금 `Update` 에 넣었어요?" 라고 물으면
> "지금은 Gizmos 로 **매 프레임 보고 싶어서**" 라고 답한다. 실제 이유다.

---

## 01:00–01:35 · 개인 미션

### 필수 미션 — 자동 조준

```csharp
using UnityEngine;

public class AutoGun : MonoBehaviour
{
    [SerializeField] private float range = 8f;

    public Transform CurrentTarget { get; private set; }

    private void Update()
    {
        CurrentTarget = FindNearest();
    }

    private Transform FindNearest()
    {
        // TODO ①: 사거리 안의 Collider 를 전부 가져오세요


        // TODO ②: 최솟값과 "누구였는지" 를 담을 변수 두 개
        //   힌트: float.MaxValue 로 시작


        // TODO ③: 하나씩 보면서
        //   - Enemy 태그가 아니면 건너뛴다
        //   - 거리를 재서 지금까지보다 가까우면 갈아치운다


        // TODO ④: 찾은 적을 돌려준다
    }

    private void OnDrawGizmosSelected()
    {
        // TODO ⑤: 사거리를 하늘색 원으로 그리세요


        // TODO ⑥: 조준 중인 적이 있으면 빨간 선을 그으세요
    }
}
```

<details>
<summary>막히면 열기 (정답)</summary>

```csharp
    private Transform FindNearest()
    {
        Collider2D[] hits = Physics2D.OverlapCircleAll(transform.position, range);

        float minDistance = float.MaxValue;
        Transform nearest = null;

        foreach (Collider2D hit in hits)
        {
            if (!hit.CompareTag("Enemy")) continue;

            float distance = Vector2.Distance(transform.position, hit.transform.position);

            if (distance < minDistance)
            {
                minDistance = distance;
                nearest = hit.transform;
            }
        }

        return nearest;
    }

    private void OnDrawGizmosSelected()
    {
        Gizmos.color = Color.cyan;
        Gizmos.DrawWireSphere(transform.position, range);

        if (CurrentTarget != null)
        {
            Gizmos.color = Color.red;
            Gizmos.DrawLine(transform.position, CurrentTarget.position);
        }
    }
```

</details>

**확인 조건**: Scene 뷰에서 `Player` 를 선택하면 하늘색 원이 보인다.
빨간 선이 **항상 가장 가까운 적**을 가리킨다. 사거리 밖으로 나가면 선이 사라진다.

### ⭐ 도전 미션

- [ ] `float.MaxValue` 를 `0f` 로 바꿔 실행 → **왜 아무도 안 잡히는지** 한 줄로 쓴다
- [ ] `range` 를 `3` 으로 줄여 조준이 자주 끊기는 걸 본다
- [ ] `OnDrawGizmosSelected` 를 `OnDrawGizmos` 로 바꿔본다 — 차이를 쓴다
- [ ] **가장 먼 적**을 조준하게 바꾼다 (부등호만 뒤집으면 된다)
- [ ] **체력이 가장 낮은 적**을 조준하게 바꾼다 (비교 기준을 바꾼다)
- [ ] 조준 중인 적의 이름을 화면 HUD 에 표시한다
- [ ] 조준선 색을 거리에 따라 바꾼다 (가까우면 빨강, 멀면 노랑)

> 💡 네 번째·다섯 번째가 **오늘의 진짜 교훈**이다. `if` 안의 비교 기준만 바꾸면
> 조준 방식이 통째로 바뀐다. Phase 6 업그레이드에서 이걸 쓴다.

---

## 01:35–01:50 · 데모 + 정리

**마무리 정리 (한 장)**

| 최솟값 찾기 3단계 | |
|---|---|
| ① | 아주 큰 값으로 시작 (`float.MaxValue`) |
| ② | 하나씩 보면서 더 작으면 **갈아치운다** |
| ③ | 값뿐 아니라 **누구였는지**도 기록 |

> 💬 "4주차 콘솔에서 배운 걸 **네 번째로** 쓰고 있습니다."
> 💬 "그때 '이걸 왜 배우지' 했던 게 지금 **총의 조준 장치**가 됐어요."

---

## ✅ 체크리스트 (학생)

- [ ] 사거리 안만 훑는다 (씬 전체가 아니다)
- [ ] `Enemy` 태그만 본다
- [ ] `float.MaxValue` 로 시작한다
- [ ] 값과 **누구였는지**를 같이 기록한다
- [ ] 적이 없으면 `null` 을 돌려준다
- [ ] Scene 뷰에 사거리 원이 보인다
- [ ] 조준선이 가장 가까운 적을 가리킨다
- [ ] 제출했다

## 🚨 흔한 사고 & 대응 (강사용)

| 에러/상황 | 진짜 원인 | 대응 |
|---|---|---|
| **아무도 조준 안 됨** | `float.MaxValue` 대신 `0f` | **오늘 1등.** 아무도 0보다 작을 수 없다 |
| 항상 같은 적만 조준 | `minDistance` 를 갱신 안 함 | `if` 안에서 둘 다 바꾼다 |
| `NullReferenceException` | `null` 검사 없이 `CurrentTarget.position` | Gizmos 에서 `!= null` |
| **Gizmo 가 안 보임** | Game 뷰를 보고 있음 | **Scene 뷰**로. 오늘 2등 |
| Gizmo 가 안 보임 (2) | `Player` 를 선택 안 함 | `...Selected` 는 선택해야 나온다 |
| Gizmo 가 안 보임 (3) | Scene 뷰 Gizmos 토글 꺼짐 | Scene 뷰 오른쪽 위 |
| 칼이나 바닥까지 잡힘 | 태그 검사 없음 | `CompareTag("Enemy")` |
| 조준선이 뚝뚝 끊김 | 정상. 적이 죽으면 다시 찾는다 | 설명해준다 |
| 프레임이 떨어짐 | 매 프레임 100마리 훑음 | 정상. 079에서 코루틴으로 옮긴다 |
| 사거리가 화면보다 큼 | `range` 가 너무 크다 | `8` 정도 (화면 반폭 8.9) |

## 📮 다음 시간 예고

> "조준은 되는데 **아직 안 쏩니다.** 답답하죠."
>
> "다음 시간에 **쏩니다.** 총알은 047에서 만들어봤죠? 거의 그대로예요."
>
> "그리고 하나 더 붙습니다. **관통** — 총알 하나가 두 마리를 뚫습니다.
> 몬스터가 100마리인데 한 발에 한 마리면 감당이 안 되거든요."
