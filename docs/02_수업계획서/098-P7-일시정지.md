# 098회차 · 일시정지 — timeScale 의 주인은 하나여야 한다

| | |
|---|---|
| **Phase** | 7 · UI · 연출 · 사운드 |
| **소요** | 2시간 (비대면) |
| **선행** | 084회차 (`timeScale`), 097회차 (씬 전환), 096회차 (UI 배치) |
| **오늘 배우는 것** | 상태로 관리하기, 책임을 한 곳에 모으기 |
| **씬** | `Game.unity` |
| **준비물(강사)** | **실제로 난 버그 로그** (아래 ⑤ 블록) |

## 🎯 오늘의 목표

1. `ESC` 로 멈추고 다시 `ESC` 로 푼다
2. 멈춘 동안 **타이틀로 나갈 수 있다**
3. **`timeScale` 을 만지는 곳을 한 곳으로 모은다**

> ⚠️ **오늘 안 하는 것**: 설정 화면(볼륨·해상도). 계속하기와 타이틀로, 두 개면 충분하다.
>
> 🔑 **설계 의도**: 일시정지 자체는 **10분이면 끝난다.** 084에서 이미 다 배웠다.
> 오늘의 진짜 내용은 **"같은 값을 여러 곳에서 만지면 서로 덮어쓴다"** 이다.
> 강사가 실제로 겪은 버그를 그대로 재현해서 보여준다.

## 📦 오늘의 제출물

**일시정지 → 계속하기 → 일시정지 → 타이틀로 가는 영상 (30초)** → `#제출`

---

## ⏱ 타임테이블

| 시간 | 블록 |
|---|---|
| 00:00–00:10 | 복습 퀴즈 + 문제 보기 |
| 00:10–00:50 | 같이 하기 — 패널 → ESC → **충돌 버그** → 정리 |
| 00:50–01:00 | 휴식 |
| 01:00–01:35 | 개인 미션 |
| 01:35–01:50 | 데모 · 정리 |
| 01:50–02:00 | 체크리스트 + 제출 + 예고 |

---

## 00:00–00:10 · 복습 퀴즈 + 문제

### 복습 퀴즈 (4분)

1. 씬을 넘겨도 안 사라지는 건? → **`static`** (097)
2. 게임을 멈추는 값은? → **`Time.timeScale = 0`** (084)
3. 멈췄을 때도 도는 것은? → **`Update`** (멈추는 건 물리와 `deltaTime`) (084)

### 문제를 본다 (6분)

▶ Play → 한참 진행한다.

> 💬 "지금 20분째 잘하고 있는데 택배가 왔습니다. 어떡하죠?"
> 💬 "**끄는 수밖에 없습니다.** 20분이 날아가요."

> 💬 "그리고 잘못 시작했을 때 **타이틀로 돌아갈 방법도 없습니다.**"

---

## 00:10–00:50 · 같이 하기 ★

### ① 패널을 만든다 (10분)

096에서 레벨업 창을 만든 것과 **똑같은 방식**이다.

```
HUD
 ├ LevelUpPanel
 ├ ExpBar · HealthBar · LevelLabel · TimeLabel · KillLabel
 └ PausePanel          ← 새로. 맨 아래 = 화면에서 맨 위
     ├ Title           "일시정지"
     ├ Btn_Resume      "계속하기"   파랑
     └ Btn_Title       "타이틀로"   회색
```

| 오브젝트 | 값 |
|---|---|
| `PausePanel` | 사방 Stretch · 색 `(0.03, 0.03, 0.06, 0.86)` · Raycast Target **켬** |
| `Title` | 가운데 · Pos `(0, 120)` · 크기 54 |
| `Btn_Resume` | 가운데 · Pos `(0, 10)` · 320×66 |
| `Btn_Title` | 가운데 · Pos `(0, -70)` · 320×66 |

> 🔑 💬 "레벨업 창은 HUD **아래**에 뒀는데, 일시정지 창은 **위**에 둡니다."
> 💬 "고를 땐 내 체력이 보여야 하지만, **멈췄을 땐 볼 이유가 없거든요.**"

> 🚨 **강사가 겪은 것**: 버튼을 만들었는데 **글자가 안 보였다.**
> 버튼 배경만 만들고 `Label` 에 **글자를 안 넣은** 것이었다.
> 💬 "네모만 두 개 떠 있으면 이걸 먼저 의심하세요."

### ② `PauseView` (10분)

```csharp
using UnityEngine;
using UnityEngine.SceneManagement;

public class PauseView : MonoBehaviour
{
    [SerializeField] private GameObject panel;
    [SerializeField] private LevelUpView levelUpView;

    public bool IsOpen => panel != null && panel.activeSelf;

    private void Start()
    {
        if (panel != null) panel.SetActive(false);
    }

    private void Update()
    {
        if (!Input.GetKeyDown(KeyCode.Escape)) return;

        // 게임이 끝난 뒤엔 안 먹는다.
        if (GameManager.Instance != null && GameManager.Instance.IsFinished) return;

        // 레벨업 창이 떠 있는데 ESC 를 누르면 둘 다 timeScale 을 만지며 싸운다.
        if (levelUpView != null && levelUpView.IsOpen) return;

        if (IsOpen) Resume();
        else Open();
    }

    public void Open()
    {
        if (panel != null) panel.SetActive(true);

        // 멈추는 일은 GameManager 가 한다. timeScale 주인은 하나여야 한다.
        if (GameManager.Instance != null) GameManager.Instance.ChangeState(GameState.Paused);
    }

    public void Resume()
    {
        if (panel != null) panel.SetActive(false);

        if (GameManager.Instance != null) GameManager.Instance.ChangeState(GameState.Playing);
    }

    public void GoTitle()
    {
        // 이걸 빼면 타이틀 화면이 멈춘 채로 뜬다.
        Time.timeScale = 1f;

        SceneManager.LoadScene("Title");
    }
}
```

| 조각 | 뜻 |
|---|---|
| `Input.GetKeyDown` | 누른 **그 순간** 한 번 (040 회수) |
| `if (IsOpen) Resume(); else Open();` | 같은 키로 켜고 끈다 (**토글**) |
| `IsFinished` → `return` | 죽은 뒤에 멈추는 건 의미가 없다 |
| `levelUpView.IsOpen` → `return` | 창 두 개가 겹치는 걸 막는다 |
| `GoTitle` 의 `timeScale = 1f` | 097과 같은 사고 |

> 💬 "`Update` 안에 `Input` 이 있죠. **멈춰 있어도 `Update` 는 돕니다.** 084에서 배운 그것이에요."
> 💬 "그래서 ESC 로 **다시 풀 수 있는** 겁니다."

### ③ `GameManager` 가 멈춤을 맡는다 (8분)

```csharp
public bool IsFinished => State == GameState.GameOver || State == GameState.Clear;

// 멈춰야 하는 상태인가. 끝났거나, 일시정지거나, 업그레이드 고르는 중이거나.
private bool ShouldFreeze => IsFinished || State == GameState.Paused || State == GameState.Upgrading;

public void ChangeState(GameState next)
{
    State = next;
    Debug.Log("게임 상태: " + next);

    Time.timeScale = ShouldFreeze ? 0f : 1f;

    if (IsFinished) GoResult();
}
```

> 💬 "**066회차에 만든 `GameState` 에 `Paused` 와 `Upgrading` 이 이미 있었습니다.**"
> 💬 "32주 만에 둘 다 씁니다."

### ④ 🐛 실제로 난 버그 (12분) ← **오늘의 핵심**

검증하다가 이런 로그가 나왔다.

```
레벨업 창 열림   →  창 = True   timeScale = 0    ← 정상
(그 사이 플레이어가 맞아 히트스톱 발생)
1초 뒤          →  창 = True   timeScale = 1    ← 🚨 창은 떠 있는데 게임이 돈다
```

> 💬 "창은 떠 있는데 **뒤에서 게임이 계속 돌아갑니다.** 카드를 고르는 동안 맞아서 죽어요."

**원인을 같이 찾는다.** 그때 `timeScale` 을 만지던 곳이 **세 군데**였다.

| 누가 | 언제 | 무엇을 |
|---|---|---|
| `LevelUpView.Open` | 레벨업 | `timeScale = 0` |
| `PauseView.Open` | ESC | `timeScale = 0` |
| `GameManager.HitStop` | 맞았을 때 | `0` 으로 했다가 **`1` 로 되돌림** |

> 🔑 💬 "히트스톱이 **끝나면서 `1` 로 되돌립니다.** 그런데 그때 레벨업 창이 떠 있었어요."
> 💬 "히트스톱은 레벨업 창이 있는지 **모릅니다.** 알 방법도 없고요."

**고친 방법** — 각자 만지지 말고 **상태만 알린다.**

| 전 | 후 |
|---|---|
| `LevelUpView`: `Time.timeScale = 0f` | `ChangeState(GameState.Upgrading)` |
| `PauseView`: `Time.timeScale = 0f` | `ChangeState(GameState.Paused)` |
| `HitStop` 끝: `Time.timeScale = 1f` | **`if (!ShouldFreeze)`** 일 때만 `1f` |

```csharp
private IEnumerator HitStopRoutine(float seconds)
{
    inHitStop = true;
    Time.timeScale = 0f;

    yield return new WaitForSecondsRealtime(seconds);

    if (!ShouldFreeze) Time.timeScale = 1f;   // 멈춰 있어야 할 상황이면 안 건드린다

    inHitStop = false;
}
```

**재측정**

```
레벨업 창 열림              state = Upgrading   timeScale = 0
그 상태에서 HitStop 호출  →  timeScale = 0   ✅
1초 뒤                    →  timeScale = 0   state = Upgrading   ✅
```

> 🔑 💬 "**같은 값을 여러 곳에서 만지면 반드시 이런 일이 납니다.**"
> 💬 "고치는 방법은 하나예요. **주인을 정하는 것.**"
> 💬 "이제 `timeScale` 을 대입하는 곳은 `GameManager` 안에만 있습니다."

### ⑤ 확인 (5분)

▶ Play → ESC → 멈춘다 → ESC → 풀린다 → ESC → 타이틀로.

**재본 값**

```
ESC 열기       →  state = Paused    timeScale = 0
[계속하기] 클릭 →  state = Playing   timeScale = 1
```

> 💡 클릭은 **진짜 마우스 클릭**으로 확인했다 — `EventSystem` 을 통해 `Btn_Resume` 이 눌린다.

---

## 01:00–01:35 · 개인 미션

### 필수 미션

```
// 씬 작업
// TODO ①: PausePanel 을 만든다 (096과 같은 방식, HUD 맨 아래에)
// TODO ②: Title / Btn_Resume / Btn_Title 을 배치하고 글자를 넣는다
```

```csharp
// PauseView.cs
// TODO ③: panel 과 levelUpView 를 SerializeField 로 받으세요
// TODO ④: IsOpen 프로퍼티를 만드세요
// TODO ⑤: Start 에서 패널을 끄세요
// TODO ⑥: Update 에서 ESC 를 받아 토글하세요
//         - 게임이 끝났으면 무시
//         - 레벨업 창이 떠 있으면 무시
// TODO ⑦: Open / Resume 은 GameManager.ChangeState 만 부르세요 (timeScale 직접 금지)
// TODO ⑧: GoTitle 에서 timeScale 을 1 로 되돌리고 Title 씬을 부르세요

// GameManager.cs
// TODO ⑨: ShouldFreeze 를 만들고 ChangeState 에서 쓰세요
// TODO ⑩: HitStop 이 끝날 때 ShouldFreeze 면 timeScale 을 안 건드리게 하세요

// LevelUpView.cs
// TODO ⑪: Time.timeScale 대입을 없애고 ChangeState(Upgrading / Playing) 으로 바꾸세요
```

<details>
<summary>막히면 열기 (정답)</summary>

코드는 위 ②·③·④ 블록 그대로다.

**버튼 배선**

| 버튼 | On Click |
|---|---|
| `Btn_Resume` | `PauseView.Resume` |
| `Btn_Title` | `PauseView.GoTitle` |

`HUD` 오브젝트를 끌어다 놓고 함수를 고른다. `PauseView` 는 `HUD` 에 붙어 있다.

**`PauseView` 의 Inspector**

| 칸 | 넣을 것 |
|---|---|
| Panel | `PausePanel` |
| Level Up View | `HUD` (자기 자신. `LevelUpView` 도 여기 붙어 있다) |

**버튼 글자가 안 보이면**
`Btn_Resume/Label` 을 클릭해 `Text` 칸에 "계속하기" 를 넣었는지 본다.
**강사도 이걸 빠뜨렸다.**

**ESC 가 안 먹으면**
- `PausePanel` 이 켜진 채 저장됐는지 (Start 가 꺼주지만 헷갈린다)
- `PauseView` 가 `HUD` 에 붙어 있는지
- Game 뷰를 **한 번 클릭**했는지 (포커스가 없으면 키 입력이 안 간다)

</details>

**확인 조건**: ESC 로 멈추고 풀리며, **레벨업 창이 떠 있을 땐 ESC 가 안 먹는다.**

### ⭐ 도전 미션

- [ ] `HitStop` 의 `if (!ShouldFreeze)` 를 빼고 레벨업 중에 맞아본다 (되돌린다)
- [ ] `levelUpView.IsOpen` 검사를 빼고 레벨업 중에 ESC 를 눌러본다 (되돌린다)
- [ ] 일시정지 창에 **지금까지의 성적**(시간·처치·레벨)을 보여준다
- [ ] `Btn_Title` 을 누르기 전에 **"정말 나가시겠습니까?"** 를 한 번 더 묻는다
- [ ] 일시정지 중에 **효과음 볼륨 슬라이더**를 넣는다 (100회차 선행)
- [ ] `ESC` 대신 `P` 로도 되게 한다
- [ ] 일시정지 창이 뜰 때 화면이 **서서히 어두워지게** 한다 (`unscaledDeltaTime`)

> 💡 첫 번째와 두 번째가 오늘 배운 걸 몸으로 확인시킨다. **전원이** 한다.

---

## 01:35–01:50 · 데모 · 정리

학생 한 명이 **레벨업 중 ESC** 를 눌러 아무 일도 안 나는 것을 보여준다.

> 💬 "**아무 일도 안 일어나는 게 정답**입니다. 그걸 만드는 데 코드 한 줄이 들었어요."

**칠판 요약**

```
같은 값을 여러 곳에서 만지면  →  서로 덮어쓴다

고치는 법 = 주인을 정한다
  timeScale 의 주인   =  GameManager
  나머지는 상태만 알린다  ChangeState(Paused / Upgrading / Playing)

멈췄어도 Update 는 돈다  →  그래서 ESC 로 풀 수 있다
```

> 💬 "이건 유니티 얘기가 아닙니다. **프로그램을 짤 때 계속 나오는 문제**예요."
> 💬 "'이 값을 누가 책임지는가.' 앞으로 계속 물어야 하는 질문입니다."

---

## ✅ 체크리스트 (학생)

- [ ] ESC 로 멈춘다
- [ ] 다시 ESC 로 풀린다
- [ ] [계속하기] 가 동작한다
- [ ] [타이틀로] 가 동작하고, **타이틀이 안 멈춰 있다**
- [ ] 죽은 뒤엔 ESC 가 안 먹는다
- [ ] **레벨업 창이 떠 있을 땐 ESC 가 안 먹는다**
- [ ] 코드에서 `Time.timeScale =` 을 검색하면 **`GameManager` 안에만** 나온다
- [ ] 제출했다

## 🚨 흔한 사고 & 대응 (강사용)

| 에러/상황 | 진짜 원인 | 대응 |
|---|---|---|
| **버튼에 글자가 없음** | Label 의 Text 를 안 채웠다 | **강사도 겪었다.** 오늘 1등 |
| ESC 가 안 먹음 | Game 뷰에 포커스가 없다 | 화면을 한 번 클릭 |
| 풀리지 않음 | `Resume` 배선 누락 / 토글 로직 | `if (IsOpen)` 확인 |
| **타이틀이 멈춰 있음** | `GoTitle` 의 `timeScale` | 097·084와 같은 사고. **네 번째** |
| 레벨업 중 ESC 로 꼬임 | `IsOpen` 검사 누락 | 오늘의 수업 내용 |
| **창은 떠 있는데 게임이 돎** | 히트스톱이 풀어버림 | **오늘의 수업 내용** |
| 시작하자마자 창이 떠 있음 | 켠 채로 저장 | 096과 같은 사고 |
| 버튼이 안 눌림 | 글자의 Raycast Target | 096 회수 |
| 죽었는데 ESC 가 먹음 | `IsFinished` 검사 누락 | |
| 창이 HUD 뒤에 있음 | Hierarchy 순서 | 맨 아래로 |

## 📮 다음 시간 예고

> "이제 게임의 **뼈대**는 다 됐습니다. 시작·진행·멈춤·끝."
>
> "그런데 아직 **때리는 느낌이 없어요.** 몬스터가 그냥 조용히 사라집니다."
>
> "다음 시간엔 **타격감**을 만듭니다.
> 화면이 흔들리고, 아주 잠깐 시간이 멈추고, 죽은 자리에 조각이 튑니다.
> **기능은 하나도 안 늘어나는데** 완전히 다른 게임이 됩니다."
