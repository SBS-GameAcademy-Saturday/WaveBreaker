# 099회차 · 타격감 — 셰이크 · 히트스톱 · 이펙트

| | |
|---|---|
| **Phase** | 7 · UI · 연출 · 사운드 |
| **소요** | 2시간 (비대면) |
| **선행** | 098회차 (`ShouldFreeze`), 077회차 (각도 분배), 051회차 (코루틴) |
| **오늘 배우는 것** | `unscaledDeltaTime`, `WaitForSecondsRealtime`, 정렬 레이어와 이펙트 |
| **씬** | `Game.unity` |
| **준비물(강사)** | ⚠️ **연출 전/후 영상 2개** · 셰이크 수치 과다 예시 |

## 🎯 오늘의 목표

1. 맞으면 **화면이 흔들린다**
2. 맞으면 **아주 잠깐 멈춘다** (히트스톱)
3. 몬스터가 죽은 자리에 **조각이 튄다**

> ⚠️ **오늘은 1시간만 쓴다.** 연출은 재밌어서 끝없이 만지게 된다.
> 남은 시간은 개인 미션과 **수치 조정**에 쓴다. 나머지는 ⭐도전이다.
>
> 🔑 **설계 의도**: 세 가지 다 **"멈춰 있어도 돌아야 한다"** 는 공통 문제를 갖는다.
> `Time.deltaTime` 이 0 인 상황에서 어떻게 움직이게 하는가 — 그게 오늘의 문법이다.

## 📦 오늘의 제출물

**연출 전 / 후를 나란히 붙인 GIF (각 5초)** → `#자랑`

---

## ⏱ 타임테이블

| 시간 | 블록 |
|---|---|
| 00:00–00:10 | 복습 퀴즈 + 문제 보기 |
| 00:10–00:50 | 같이 하기 — 셰이크 → 히트스톱 → 번쩍임 → 조각 |
| 00:50–01:00 | 휴식 |
| 01:00–01:30 | 개인 미션 |
| 01:30–01:50 | **수치 조정 실습** + 데모 |
| 01:50–02:00 | 체크리스트 + 제출 + 예고 |

---

## 00:00–00:10 · 복습 퀴즈 + 문제

### 복습 퀴즈 (4분)

1. `timeScale` 의 주인은? → **`GameManager`** (098)
2. 멈췄을 때 `Time.deltaTime` 은? → **0** (084)
3. 칼 4자루를 360도에 배치하려면? → **`360 / 개수 * i`** (077)

### 문제를 본다 (6분)

▶ Play → 몬스터를 잡는다. 맞아본다.

> 💬 "지금 몬스터가 죽으면 어떻게 됩니까?" → **"그냥 사라져요."**
> 💬 "맞으면요?" → **"체력바 숫자가 줄어요."**
>
> 💬 "**아무 느낌이 없죠.** 스프레드시트 같아요."

**상용 게임을 보여준다.** 뱀서라이크류의 타격 순간을 프레임 단위로 본다.

| 요소 | 시간 |
|---|---|
| 화면 흔들림 | 0.15초 |
| 시간 정지 | 0.06초 |
| 흰색 번쩍임 | 0.06초 |
| 조각 튀기 | 0.35초 |

> 🔑 💬 "**전부 0.5초 미만입니다.** 길면 오히려 답답해져요."

---

## 00:10–00:50 · 같이 하기 ★

### ① 카메라 셰이크 (12분)

`CameraFollow` 에 **넣는다.** 새 스크립트를 안 만든다.

```csharp
private float shakeLeft;
private float shakePower;

void LateUpdate()
{
    if (targets.Count == 0) return;

    Vector3 center = Center();
    Vector3 goal = new Vector3(center.x, center.y, -10f);

    transform.position = Vector3.SmoothDamp(transform.position, goal, ref velocity, smoothTime);

    // 따라간 다음에 흔든다. 순서가 반대면 흔든 걸 따라가기가 지워버린다.
    transform.position += ShakeOffset();
}

public void Shake(float power, float duration)
{
    // 약한 흔들림이 강한 흔들림을 덮어쓰면 안 된다.
    if (duration <= shakeLeft && power <= shakePower) return;

    shakeLeft = duration;
    shakePower = power;
}

private Vector3 ShakeOffset()
{
    if (shakeLeft <= 0f) return Vector3.zero;

    // 히트스톱 중에도 흔들려야 한다. timeScale 을 안 타는 시간을 쓴다.
    shakeLeft -= Time.unscaledDeltaTime;

    if (shakeLeft <= 0f) return Vector3.zero;

    return (Vector3)Random.insideUnitCircle * shakePower;
}
```

> 🔑 💬 "**왜 `CameraShake` 스크립트를 따로 안 만들었을까요?**"
> 💬 "따로 만들면 '따라가기가 먼저냐 흔들기가 먼저냐' 를 매번 신경 써야 합니다."
> 💬 "같은 `LateUpdate` 안에서 순서대로 하면 그 걱정이 **아예 없어져요.**"

| 조각 | 뜻 |
|---|---|
| `Random.insideUnitCircle` | 반지름 1인 원 안의 아무 점 |
| `* shakePower` | 흔들리는 크기 |
| `Time.unscaledDeltaTime` | **`timeScale` 을 안 타는 시간** ← 오늘의 문법 |
| `if (duration <= shakeLeft && power <= shakePower)` | 약한 게 강한 걸 덮지 않게 |

> 🚨 **`Time.deltaTime` 을 쓰면 히트스톱 중에 흔들림이 멈춘다.**
> 그러면 "때린 순간" 이 그냥 정지 화면이 된다. **둘이 같이 일어나야** 느낌이 난다.

### ② 히트스톱 (10분)

`GameManager` 에 넣는다. `timeScale` 의 주인이니까(098).

```csharp
private bool inHitStop;

public void HitStop(float seconds)
{
    if (ShouldFreeze) return;
    if (inHitStop) return;   // 겹치면 게임이 영영 멈춘다

    StartCoroutine(HitStopRoutine(seconds));
}

private IEnumerator HitStopRoutine(float seconds)
{
    inHitStop = true;

    Time.timeScale = 0f;

    // WaitForSeconds 는 timeScale 이 0 이면 영원히 안 끝난다. Realtime 을 쓴다.
    yield return new WaitForSecondsRealtime(seconds);

    if (!ShouldFreeze) Time.timeScale = 1f;

    inHitStop = false;
}
```

> 🔑 💬 "**`WaitForSeconds` 를 쓰면 게임이 영영 멈춥니다.**"
> 💬 "`timeScale = 0` 이면 게임 시간이 안 흐르고, 그래서 `WaitForSeconds(0.06f)` 가 **끝나질 않아요.**"
> 💬 "`WaitForSecondsRealtime` 은 **진짜 시계**를 봅니다."

▶ 일부러 `WaitForSeconds` 로 바꿔 실행해 보여준다. 게임이 얼어붙는다.

| 검사 | 왜 |
|---|---|
| `if (ShouldFreeze) return` | 이미 멈춰 있으면 안 한다 |
| `if (inHitStop) return` | 두 번 겹치면 하나가 먼저 `1f` 로 되돌린다 |
| `if (!ShouldFreeze)` (끝날 때) | **098의 그 버그.** 레벨업 창을 풀어버리면 안 된다 |

### ③ 어디서 부르나 (8분)

> 💬 "**몬스터가 맞을 때마다 화면을 흔들면** 어떻게 될까요?"
> 💬 "칼이 4개, 총알이 관통합니다. 초당 수십 번이에요. **못 봅니다.**"

**우리 규칙**

| 언제 | 연출 |
|---|---|
| 몬스터가 맞을 때 | 흰색 번쩍임 + 작은 타격음 |
| 몬스터가 죽을 때 | 조각 + 처치음 |
| **플레이어가 맞을 때** | **셰이크 + 히트스톱** + 피격음 |

`PlayerHealth.TakeDamage` 안에 넣는다.

```csharp
// 연출은 플레이어가 맞을 때만 크게 준다. 몬스터가 맞을 때마다 화면을 흔들면 못 본다.
if (cam != null) cam.Shake(shakePower, shakeTime);
if (GameManager.Instance != null) GameManager.Instance.HitStop(hitStopTime);
```

| 값 | 우리 설정 |
|---|---|
| `shakePower` | **0.12** |
| `shakeTime` | **0.15** |
| `hitStopTime` | **0.06** |

> 🔑 💬 "**연출은 과하면 독입니다.** `shakePower` 를 `1.0` 으로 해보면 알아요."

### ④ 번쩍임 (6분)

`Enemy` 에 넣는다.

```csharp
protected void Flash()
{
    if (sprite == null || !gameObject.activeInHierarchy) return;

    StopCoroutine(nameof(FlashRoutine));
    StartCoroutine(nameof(FlashRoutine));
}

private IEnumerator FlashRoutine()
{
    Color original = data != null ? data.color : Color.white;

    sprite.color = Color.white;

    // 히트스톱 중에도 번쩍여야 한다. timeScale 을 안 타는 대기를 쓴다.
    yield return new WaitForSecondsRealtime(0.06f);

    if (sprite != null) sprite.color = original;
}
```

> 💬 "원래 색을 **`EnemyData` 에서 읽습니다.** 088에서 빼둔 게 여기서 값을 하죠."
> 🚨 **`StopCoroutine` 을 먼저 부르는 이유**: 연속으로 맞으면 코루틴이 여러 개 돈다.
> 먼저 것이 끝나면서 원래 색으로 되돌려 **번쩍임이 끊긴다.**

### ⑤ 죽을 때 조각 (10분) — 실측 이야기 포함

`Scripts/Effect/DeathEffect.cs`

```csharp
public class DeathEffect : MonoBehaviour
{
    [SerializeField] private Sprite shape;
    [SerializeField] private int count = 8;
    [SerializeField] private float speed = 4.5f;
    [SerializeField] private float life = 0.35f;
    [SerializeField] private float size = 0.22f;
    [SerializeField] private Color color = new Color(1f, 0.72f, 0.35f);

    private SpriteRenderer[] shards;
    private Vector2[] dirs;
    private float elapsed;

    private void Start()
    {
        shards = new SpriteRenderer[count];
        dirs = new Vector2[count];

        for (int i = 0; i < count; i++)
        {
            // 077회차의 칼 각도 분배와 같은 계산이다.
            float angle = 360f / count * i + Random.Range(-14f, 14f);
            dirs[i] = new Vector2(Mathf.Cos(angle * Mathf.Deg2Rad), Mathf.Sin(angle * Mathf.Deg2Rad));

            GameObject go = new GameObject("Shard");
            go.transform.SetParent(transform, false);

            SpriteRenderer sr = go.AddComponent<SpriteRenderer>();
            sr.sprite = shape;
            sr.color = color;
            sr.sortingLayerName = "Effect";   // 이걸 빼면 바닥 타일 뒤에 그려져서 안 보인다
            go.transform.localScale = Vector3.one * size;

            shards[i] = sr;
        }
    }

    private void Update()
    {
        elapsed += Time.deltaTime;

        float k = elapsed / life;   // 0 에서 1 로 간다

        if (k >= 1f) { Destroy(gameObject); return; }

        for (int i = 0; i < shards.Length; i++)
        {
            shards[i].transform.localPosition = (Vector3)(dirs[i] * speed * elapsed);

            Color c = color;
            c.a = 1f - k;
            shards[i].color = c;

            shards[i].transform.localScale = Vector3.one * size * (1f - k * 0.6f);
        }
    }
}
```

> 🔑 💬 "**`sortingLayerName = "Effect"` 가 핵심입니다.**"
> 💬 "045회차에서 정렬 레이어를 만들었죠. `Background / Ground / Enemy / Player / Effect` 순서요."
> 💬 "`Default` 로 두면 **`Background` 보다도 아래**입니다. 바닥 타일에 가려서 안 보여요."

### ⑥ 🐛 강사가 겪은 것 — 파티클을 포기했다 (4분)

원래 `ParticleSystem` 으로 만들려고 했다. 그런데 **화면에 안 나왔다.**

```
입자 24개 살아 있음 · isVisible = True · 머티리얼 있음 · 위치도 화면 안
그런데 화면에는 아무것도 없다
```

정렬 레이어를 `Effect` 로 고치고, 머티리얼에 텍스처를 물리고, 크기를 두 배로 키워도
**끝까지 안 나왔다.** 원인을 못 찾았다.

**판단**: 스프라이트로 갈아탔다. 스프라이트는 이 게임의 **모든 것이 쓰는 방식**이라 확실히 나온다.

> 🔑 💬 "**안 되는 걸 붙잡고 있기보다, 되는 걸로 만듭니다.**"
> 💬 "40분짜리 회차에서 원인 모를 렌더링 문제를 파는 건 좋은 선택이 아니에요."
> 💬 "파티클로 다시 해보는 건 **도전 과제**로 남겼습니다."

> 💡 이건 실력의 문제가 아니라 **시간 배분의 문제**다. 학생에게 이 판단 자체를 가르친다.

---

## 01:00–01:30 · 개인 미션

### 필수 미션

```csharp
// CameraFollow.cs
// TODO ①: shakeLeft / shakePower 필드를 추가하세요
// TODO ②: Shake(power, duration) 을 만드세요 (약한 게 강한 걸 못 덮게)
// TODO ③: ShakeOffset() 을 만들고 LateUpdate 맨 끝에서 더하세요
//         - Time.unscaledDeltaTime 을 쓸 것

// GameManager.cs
// TODO ④: HitStop(seconds) 와 코루틴을 만드세요
//         - WaitForSecondsRealtime 을 쓸 것
//         - 겹치기 방지, 끝날 때 ShouldFreeze 검사

// PlayerHealth.cs
// TODO ⑤: 맞을 때 Shake(0.12, 0.15) 와 HitStop(0.06) 을 부르세요

// Enemy.cs
// TODO ⑥: Flash() 를 만들고 TakeDamage 에서 부르세요

// DeathEffect.cs (새 파일)
// TODO ⑦: 조각 8개를 360도로 만들고, 밖으로 날리며 흐려지게 하세요
//         - sortingLayerName 을 "Effect" 로
// TODO ⑧: 프리팹으로 만들고 몬스터 6종의 Death Effect 칸에 넣으세요
```

<details>
<summary>막히면 열기 (정답)</summary>

코드는 위 ①~⑤ 블록 그대로다.

**프리팹 만드는 법**
1. 빈 오브젝트를 만들고 이름을 `DeathEffect` 로
2. `DeathEffect` 스크립트를 붙인다
3. `Shape` 칸에 `UI_Bar` 스프라이트를 넣는다
4. `Assets/_Project/Prefabs/Effect/` 로 드래그
5. 씬에 있는 원본은 지운다

**몬스터에 연결**
`Enemy_Charger` · `Enemy_Runner` · `Enemy_Tank` · `Enemy_Boss1/2/3` **여섯 개 전부**.
프리팹을 더블클릭해 편집 모드에서 `Death Effect` 칸에 넣는다.

**`PlayerHealth` 의 새 칸**

| 칸 | 값 |
|---|---|
| Cam | `Main Camera` |
| Shake Power | `0.12` |
| Shake Time | `0.15` |
| Hit Stop Time | `0.06` |

**조각이 안 보이면 순서대로 확인한다**
1. `sortingLayerName` 이 `"Effect"` 인가
2. `Shape` 칸이 비어 있지 않은가
3. `size` 가 너무 작지 않은가 (`0.22` 권장)
4. 죽은 자리가 **화면 안**인가 (몬스터는 화면 밖에서 스폰된다)

</details>

**확인 조건**: 맞으면 화면이 흔들리고 잠깐 멈춘다. 몬스터를 잡으면 조각이 **눈에 보인다.**

### ⭐ 도전 미션

- [ ] `Time.unscaledDeltaTime` 을 `Time.deltaTime` 으로 바꿔 히트스톱 중 흔들림이 멈추는 걸 본다
- [ ] `WaitForSecondsRealtime` 을 `WaitForSeconds` 로 바꿔 게임이 얼어붙는 걸 본다
- [ ] `shakePower` 를 `1.0` 으로 해보고 왜 안 되는지 쓴다
- [ ] **보스가 죽을 때만** 셰이크를 크게 준다
- [ ] 조각 색을 `EnemyData.color` 에서 가져온다
- [ ] 총알이 맞은 자리에 **작은 스파크**를 띄운다
- [ ] ⭐⭐ `ParticleSystem` 으로 다시 만들어 본다 (강사가 실패한 것)

> 💡 앞의 세 개는 **전원이** 한다. 오늘 배운 세 문법을 몸으로 확인하는 자리다.
> 💡 마지막을 해낸 학생은 **강사에게 방법을 알려주게 한다.**

---

## 01:30–01:50 · 수치 조정 실습 + 데모

### 수치 조정 (12분)

Inspector 만 열고 **각자 자기 취향으로** 맞춘다.

| 값 | 범위 | 너무 크면 |
|---|---|---|
| `Shake Power` | 0.05 ~ 0.25 | 어지럽다. 화면이 뭘 보여주는지 모른다 |
| `Shake Time` | 0.1 ~ 0.3 | 계속 흔들려서 멀미가 난다 |
| `Hit Stop Time` | 0.03 ~ 0.10 | 게임이 뚝뚝 끊긴다 |
| `Count` (조각) | 6 ~ 12 | 화면이 지저분해진다 |

> 💬 "**정답이 없습니다.** 여러분이 재밌다고 느끼는 값이 정답이에요."
> 💬 "다만 **과한 쪽으로 한 번 가보세요.** 그래야 적당한 게 어딘지 압니다."

### 데모 (8분)

학생 2~3명이 자기 수치로 시연한다.

> 💬 "같은 코드인데 느낌이 다 다르죠. **이게 게임 디자인입니다.**"

---

## ✅ 체크리스트 (학생)

- [ ] 맞으면 화면이 흔들린다
- [ ] 맞으면 아주 잠깐 멈춘다
- [ ] 몬스터가 맞으면 하얗게 번쩍인다
- [ ] 몬스터가 죽으면 조각이 튄다
- [ ] 조각이 **바닥 뒤에 안 가려진다**
- [ ] 레벨업 창이 떠 있을 때 히트스톱이 **창을 안 푼다**
- [ ] 흔들림이 어지럽지 않다
- [ ] 제출했다

## 🚨 흔한 사고 & 대응 (강사용)

| 에러/상황 | 진짜 원인 | 대응 |
|---|---|---|
| **게임이 영영 멈춤** | `WaitForSeconds` 를 썼다 | **오늘 1등.** Realtime |
| **조각이 안 보임** | `sortingLayerName` 미설정 | **오늘 2등.** `"Effect"` |
| 조각이 화면 밖 | 몬스터가 화면 밖에서 죽었다 | 정상. 가까이서 잡아본다 |
| 히트스톱 중 흔들림 멈춤 | `Time.deltaTime` 을 썼다 | `unscaledDeltaTime` |
| 어지러움 | 수치 과다 | **"연출은 과하면 독"** |
| 번쩍임이 끊김 | `StopCoroutine` 누락 | 연속 피격 |
| 번쩍인 뒤 색이 흰색 | 원래 색을 안 되돌림 | `data.color` |
| 조각이 안 사라짐 | `Destroy` 누락 | `k >= 1f` |
| 카메라가 흔들린 채 굳음 | `LateUpdate` 순서 | 따라가기 → 흔들기 |
| 레벨업 창이 풀림 | `ShouldFreeze` 검사 누락 | 098 회수 |
| 파티클이 안 나옴 | 원인 미상 (강사도 못 잡음) | **스프라이트로 간다** |

## 📮 다음 시간 예고

> "눈으로 보이는 건 다 됐습니다. 그런데 **아직 조용합니다.**"
>
> "다음 시간엔 **소리**를 넣습니다. 그리고 Phase 7 을 마무리해요."
>
> "타이틀부터 결과까지 **한 판을 처음부터 끝까지** 점검합니다.
> 다음 주부터는 **빌드**입니다. 여러분이 만든 걸 친구한테 보낼 수 있게 됩니다."
